# Track all statements (optional, adjust as needed)
pg_stat_statements.track = all

# Limit the amount of query text stored (default: 1024)
# pg_stat_statements.max = 5000
# pg_stat_statements.track_utility = off

# Reset stats after this many calls (optional)
# pg_stat_statements.save = on
