{
  "name": "@syncpad/redis-client",
  "version": "0.1.0",
  "description": "Shared Redis client with reconnect/backoff helpers for SyncPad backend and worker.",
  "main": "dist/index.js",
  "type": "module",
  "types": "dist/index.d.ts",
  "packageManager": "pnpm@10.14.0",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "rimraf dist && tsc -p tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "rimraf": "^6.1.2",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "redis": "^5.10.0",
    "winston": "^3.18.3"
  }
}
